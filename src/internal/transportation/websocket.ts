module RongIMLib {
    export class WebSocket implements Transportation {

        //TODO
        connect(url: string): any {
            return undefined;
        }

        //TODO
        disconnect(): void {

        }

        //TODO
        sned(message: MessageContent): void {

        }

        //TODO
        decodeData(data: any): any {
            return undefined;
        }
    }
}
